{% extends 'base.html.twig' %}

{% block title %}Acceptation{% endblock %}

{% block body %}
    <h1>Acceptation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acceptation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ acceptation.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ acceptation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('acceptation_index') }}">back to list</a>

    <a href="{{ path('acceptation_edit', {'id': acceptation.id}) }}">edit</a>

    {{ include('acceptation/_delete_form.html.twig') }}
{% endblock %}
