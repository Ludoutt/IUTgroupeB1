{% extends 'base.html.twig' %}

{% block title %}Edit Backlog{% endblock %}

{% block body %}
    <a href="{{ path('backlog_index') }}">
        <button class="mb-3 btn btn-info">Retour</button>
    </a>

    <h1>Mettre a jour {{ backlog.name|capitalize }}</h1>

    {{ include('backlog/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('backlog/_delete_form.html.twig') }}
{% endblock %}
